# gpt_tests

This repository contains a simple web-based click counter.

Open `index.html` in a browser. Select the duration (5, 10, 30, or 60 seconds) and press **Start**. Then click the **Click me!** button as many times as possible before the timer ends. The app reports the total number of clicks when time is up.
